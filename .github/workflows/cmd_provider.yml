name: SMODERP2D CMD provider

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  consistency_test:
    strategy:
      matrix:
        python-version:
          - "2.7"
          - "3.8"
          - "3.11.2"

    uses: storm-fsv-cvut/smoderp2d/.github/workflows/cmd_template.yml@162-profile1d-fails
    with:
      python-version: ${{ matrix.python-version }}